plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    id 'com.google.gms.google-services'
}

android {
    namespace = "com.haweeinc.lpmi40"
    compileSdk = 34 // Updated to 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId "com.haweeinc.lpmi40"
        minSdkVersion 23 // Ensure this is at least 23
        targetSdkVersion 35 // Update targetSdkVersion to 34
        versionCode 9
        versionName "1.9"
    }

    signingConfigs {
        release {
            keyAlias 'upload'
            keyPassword 'advsabah'
            storeFile file('/Volumes/HHS Storage/Flutter APPS/lpmi40/upload-key.jks')
            storePassword 'advsabah'
        }
    }

    buildTypes {
        release {
            minifyEnabled true // Enables code shrinking for release
            shrinkResources true // Removes unused resources in release builds
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig = signingConfigs.release // Use release signing config
        }
        debug {
            signingConfig = signingConfigs.debug // Keeps the debug signing for debug builds
        }
    }
}

dependencies {
    implementation platform('com.google.firebase:firebase-bom:33.5.1')  // Firebase BOM (Bill of Materials) for dependency management
    implementation 'com.google.firebase:firebase-analytics-ktx'  // Example Firebase service, replace or add as needed
    // Add more Firebase dependencies here, such as Firebase Auth, Firestore, etc.
}

flutter {
    source = "../.."
}

